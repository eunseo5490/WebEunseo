20260529

---

물리 데이터저장소 구성
관계형 테이베이스 사이에서의 제약조건
PK - FK 연결되어 있는 두 개 테이블의 데이터에 대한 제약조건

삭제 했을 때(ON DELETE ...)
CASCADE : PK 값이 삭제되면 FK 쪽에서도 같이 삭제된다.
RESTRICT : FK쪽에서 사용중이면 PK쪽의 값을 삭제할 수 없다.
NULLIFY : PK쪽 값이 삭제되면 FK쪽에서는 NULL로 바꾼다.
수정했을 때(ON UPDATE ...)
CASCADE : PK쪽 값을 수정하면 FK 쪽에서도 같이 수정된다.
RESTRICT : FK쪽에서 사용중이면 PK쪽의 값을 수정할 수 없다.
NULLIFY : PK쪽 값을 수정하면 FK쪽에서는 NULL로 바꾼다.

언제쓰냐?
테이블 만들 때 외래키 제약조건 밑에 같이 속성을 작성해준다.

인덱스 설계
- 조회 성능을 높이기 위해 어떤 컬럼에 인덱스를 적용시켜줘야 할까?

인덱스 컬럼의 분포도가 10 ~ 15% 이내가 일반적이다.
분포도의 값이 적을수록 인덱스 효율이 높다 -> 분포도가 넓다

분포도 = (1 / 도메인의 수) X 100

조회 및 출력 조건으로 사용되는 컬럼인 경우
- WHERE 에서 많이 사용되는 컬럼인가

PK와 UNIQUE를 지정한 컬럼은 인덱스가 자동으로 만들어진다.

수정이 빈번하지 않은 컬럼을 선정을 한다.

자주 조합되서 사용되는 컬럼은 결합 인덱스로 생성

뷰(View)
- SELECT를 한 결과를 저장하는 가상의 테이블

뷰의 속성
REPLACE
- 뷰가 이미 존재하는 경우 기존 뷰를 새 내용으로 다시 생성하는 속성
CREATE OR REPLACE 뷰이름 AS
...

FORCE
- 기존 테이블이 존재하지 않아도 뷰를 생성하는 속성
- ORACLE에서 사용됨
- 단, 뷰를 사용하려고 하면 에러가 발생

NOFORCE
- 기본테이블이 존재할때만 뷰를 생성할 수 있게 함

WITH CHECK OPTION
- 뷰의 조건을 만족하는 행만 INSERT 또는 UPDATE할 수 있도록 제한하는 속성

WITH READ ONLY
- 뷰를 읽기 전용으로 만드는 속성

뷰 설계시 고려사항
- 결국 VIEW 안에 SELECT문을 실행하는거기 때문에 데이터가 너무 많으면 조회가 느릴 수 있다.
- 뷰 안에 조건이 있다면, 해당 조건 컬럼에 인덱스가 걸려있는지 확인

클러스터
- 자주 같이 조회되는 데이터를 디스크의 가까운 위치에 저장해서 조회 속도를 높이는 물리적 저장 방법

분포도가 넓을 때 유용하다

여러 블록 이상의 테이블에 적용한다.
블록 :  DB가 데이터를 저장할 때 블록이라는 저장 공간 단위로 저장한다.

- 데이터가 많은 경우
디스크 내에서 수천 ~ 수만개 블록으로 나뉜다.
[블록1]
주문 데이터 일부

[블록2]
주문 데이터 일부

[블록3]
주문 데이터 일부

...

관련 데이터를 가까운 블록에 모아서 저장

[블록1]
고객1 주문
고객1 주문
고객1 주문

[블록2]
고객2 주문
고객2 주문
고객2 주문

[블록3]
고객3 주문
고객3 주문
고객3 주문
고객3 주문

...

디스크 이동이 감소 -> 읽는 블록 수 감소 -> 조회 속도가 높아진다.

검색은 빨라지지만 입력, 수정, 삭제는 느려질 수 있다.

UNION, DISTINCT, ORDER BY, GROUP BY가 빈번한 컬럼이면 고려

파티션 설계
- 하나의 테이블 내에서 내부적으로 여러 조각으로 저장하는 기술
- 하나의 테이블만 사용되지만 DBMS는 데이터를 여러 저장구역으로 나누어 관리한다.

종류
범위 분할
- 기준이 사람이 설정
해시분할
- DB가 기준을 설정해서 데이터를 균등하게 분배
조합 분할
- 서브 파티션을 생성한다.

디스크 구성 설계
- 데이터를 어디에, 어떻게 저장할 것인지 계획하는 작업

정확한 용량을 산정하여 디스크 사용의 효율을 높인다.

예시
쇼핑몰 주문
하루 주문건수 10,000건
주문 1건의 크기 1KB
하루 증가량 약 10MB
1년 증가량 3.6GB

많은 작업이 발생하는 데이터를 한 곳에 몰아두지 않는다.

입출력 경합을 최소화 하여 데이터의 접근 성능을 향상시킨다.
- 입출력 경합 : 여러 작업이 같은 디스크를 동시에 사용하는 상황

테이블 객체를 위한 테이블 스페이스와 인덱스 객체를 위한 테이블스페이스를 분리 구성한다.
- 실제 데이터와 인덱스를 따로 저장한다.

물리 데이터 모델 구성
테이블명 정하기
속성명 정하기
타입과 길이 정하기
제약조건 정하기
인덱스 정하기
뷰 정하기
클러스터 만들기
파티션 정하기
디스크 구성 설계

SQLD, ADSP -> 데이터베이스 관련 자격증

MYSQL
CREATE TABLE MEMBER(
	member_id INT AUTO_INCREMENT
)

INSERT INTO MEMBER()

ORACLE
CREATE SEQUENCE member_seq
start with 1
INCREMENT BY 1;

INSERT INTO MEMBER(member_seq.nextVal)

작업단위
1. 내 잔고 차감
2. 상대방 잔고 증가

실행
결과

데이터베이스에 바로 반영하는게 아님(임시 저장)

COMMIT을 통해 데이터베이스에 반영

쿼리문 실행 결과가 내가 원하는 결과가 아님

ROLLBACK 통해 되돌린다.

VARIABLE v_count NUMBER; -> 바인드 변수 생성

EXEC get_member_count(:v_count);

PRINT v_count;





# 실습1
Java 프로그램이 MySQL 데이터베이스와 통신할 수 있도록 하는 프로그램의 이름을 작성하시오.
ㄴ JDBC


# 실습2
다음 SQL은 사용자가 입력한 부서명을 조건으로 직원을 조회하는 기능입니다.
String sql = "SELECT * FROM employee WHERE dept = ?"
위 SQL을 실행할 때 가장 적절한 Java 클래스 이름을 작성하시오.
ㄴPreparedStatement


# 실습3
[실무 시나리오]
A 인사관리 시스템에서 다음 SQL을 사용하고 있다.
String sql =
"SELECT * FROM employee WHERE id='" + id + "'";
보안 점검 결과 취약점이 발견되었다.

문제
어떤 보안 문제가 발생할 수 있는지 작성하고 해결 방법을 작성하시오.
작성해야 하는 내용
ㄴ발생 가능한 문제
ㄴ해결 방법

답
ㄴ SQL Injection


# 실습4
[실무 시나리오]
다음 코드가 실행되고 있다.
ResultSet rs = pstmt.executeQuery();

while(rs.next()){
    System.out.println(
        rs.getString("emp_name")
    );
}

문제
rs.next() 메서드의 역할을 작성하시오.

답
ㄴ 다음 행으롤 이동, 다음 행이 있으면 true, 없으면 false


# 실습5
[실무 시나리오]
주문 처리 프로그램이 다음 순서로 동작한다.
1. 주문 저장
2. 재고 감소
3. 결제 정보 저장
재고 감소 단계에서 오류가 발생하였다.
개발팀은 주문 저장도 함께 취소되기를 원한다.

문제
어떤 기술을 사용해야 하는지 작성하고 이유를 설명하시오

답
ㄴ 트랜잭션 롤백


# 실습6
[실무 시나리오]
다음 코드가 실행되고 있다.
ResultSet rs = pstmt.executeQuery();
개발자가 실수로 ResultSet을 닫지 않았다.

문제
REsultSet을 close해야 하는 이유를 작성하시오.

답
ㄴ 메모리 누수를 막기 위해서
