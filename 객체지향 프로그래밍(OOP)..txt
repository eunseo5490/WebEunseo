객체지향 프로그래밍(OOP).
객체지향이 아닐 때의 문제점
- 데이터와 기능이 따로 논다.
- 코드가 많아지게 되며 관리가 어렵다
- 재사용이 힘들다

객체지향의 핵심 개념
1. 클래스(class)
- 설계도
ex) 이 프로그램에서 사람을 어떻게 정의할 건지, 제품을 어떻게 정의할건지
클래스 안에 정의할 수 있는 내용
- 필드(field) : 각 개체가 가지는 정보(상태, 속성)
ㄴ 이름, 나이, 키, 몸무게, 혈액형, 성별, 주소, 학번 ...
- 메서트(method) : 각 객체가 할 수 있는 동작(기능)
ㄴ 공부한다, 먹는다, 운동한다, 잔다 ...

2. 객체(object)
클래스명 변수명 = new 클래스명();
- 클래스를 통해서 만들어진 것
- 필드에 객체만의 정보를 넣거나 필드의 내용을 사용할 수 있다.
객체변수명.필드명
- 클래스에 정의된 기능을 호출하여 사용할 수 있다.
객체변수명.메서드명();

3. 객체지향의 4대 특징
- 캡슐화
데이터를 보호하는 개념
필드 직접 접근하지 못하게 한다.
제공된 메서드를 통해서만 접근할 수 있게 한다.

- 상속
기존 클래스의 내용을 물려받는 기능(필드, 메서드)
코드를 재사용할 때 좋음

- 다형성
하나의 타입으로 여러 객체를 다룰 수 있는 성질
타이어마다 성능이 다르다.

- 추상화
클래스를 단순화 시키는 것

메서드 내부에서 메서드를 정의할 수 없다.
메서드 외부에서 메서드를 호출하는 것은 불가능하다.


참조용 클래스(객체를 만들기 위한 클래스)
- main 메서드 없음
- 필드
- 생성자 (호출했을 때 메모리에 객체를 올려주는 기능)
ㄴ 객체를 생성했을 때 필드를 초기화 하는 역할
정의하지 않으면 컴파일러가 기본생성자를 만들어서 사용

사용자가 직접 정의하면 컴파일러가 만든 기본생성자는 사용되지 않는다.

메서드처럼 매개변수를 가질 수 있다.

- 메서드 (여러 개 정의 가능)
접근제한자 반환타입 메서드명(매개변수){

}

메서드 (계산기)
정의 (기능을 만드는 것)
ㄴ 숫자 두 개와 연산자를 받을 준비
호출
ㄴ 숫자와 연산자를 전달하여 연산결과를 돌려받는다.


return
매개변수, 메서드 내부에서 정의된 변수는 지역변수이다.
메서드가 호출돼서 실행한다음 종료가 되면 메모리에서 삭제된다.

만약, 메서드 밖에서 연산결과를 사용하고 싶다면 return을 통해 값을 밖으로 꺼내야 한다.

메서드명
- 메서드를 호출할 때 사용하는 이름
- 첫글자는 소문자로 작성

//생성자(or메서드)를 호출한 객체의 필드임을 명시하기 위해서 this키워드를 사용한다.



오버로딩(overloading)
- 같은 이름으로 서로 다른 개수, 타입의 매개변수를 각각 처리하는 방법

- 반환형은 달라도 상관 없음
- 메서드(or생성자)명은 같아야 한다.
- 매개변수의 개수가 다르거나, 타입이 다르거나, 순서가 달라야 한다.

 

접근제한자(Access Modifier)
public : 프로젝트 내에서는 어디서든지 사용 가능
default : 같은 패키지 내의 다른 클래스 어디서든지 사용 가능
private : 현재 클래스에서만 사용 가능
protected : (상속 후)

필드/생성자/메서드/클래스에 붙힐 수 있다.



디자인 패턴
프로그래밍을 하다보면 비슷한 문제가 반복적으로 나타나는 경우가 있다.
- 객체를 하나만 만들어야 하는 경우
- 객체 생성이 너무 복잡한 경우
- 기능은 비슷한데 구현 방법이 여러개인 경우
- 객체들끼리 너무 강하게 연결되어 있어 수정이 어려운 경우

개발자들이 오랜 시간 동안 많이 쓰인 설계 방법을 정리해둔 것이 디자인 패턴이다.



상속의 특징
하나의 부모는 여러 클래스한테 상속할 수 있다.
하나의 자식이 여러 클래스를 상속받는 건 불가능


패키지
단순히 클래스를 분리하는 용도가 아니라 클래스별 구분자역할도 한다.
패키지 계층구조로 만들 수 있다.
com
  ㄴ kumho
    ㄴTire
  ㄴ hankook
    ㄴTire

보통은 회사 도메인의 역순으로 패키지 이름을 정한다.
도트(.)를 기준으로 계층이 생긴다.
com.kumho.Tire


코드를 작성할 때 완벽하게 작성
수정에 용이한 코드가 좋은 코드
너무 구체적인 코드는 좋지 않음
기능이 추가될 때 수정을 최소화하는 코드


싱글톤 패턴
객체를 메모리에 한 번만 올리고, 올라간 객체를 계속 사용하는 방식
1. 생성자를 private으로 정의해서 외부에서 호출하지 못하도록 한다.
2. 클래스 내부에서 static으로 객체를 하나 생성한다. (private)
4. 클래스 내부에서 만든 객체를 사용할 수 있도록 메서드(public)를 만든다.



상속
부모 클래스의 필드나 메서드를 자식 클래스에서 사용할 수 있는 것
자식 클래스가 부모 클래스를 고른다.



오버로딩(메서드의 중복 정의)
오버라이딩(메서드의 재정의)



클래스 타입변환
- 상속관계에서만 가능
자동타입변환 : 자식 -> 부모타입
- 자식클래스에 정의한 멤버를 사용할 수 없다.
강제타입변환 : 부모타입 -> 자식타입
- 자식클래스에 정의한 멤버를 사용할 수 있다.
(자식클래스)부모객체



생성자 : 객체를 메모리에 올리는 역할, 필드를 초기화하는 역할
메서드 : 만들고 싶은 기능, 자주 사용하는 기능을 만드는 것

this : 호출한 객체 자신
this.필드명
this()

super : 부모클래스
super.필드명
super()
super.메서드명()

오버라이딩(메서드 재정의)
상속관계에서만 사용 가능

부모클래스에 있는 메서드 이름으로 사용하고 싶지만
자식클래스의 상황에 맞지 않아서 자식클래스에서 재정의할 때

접근제한자, 반환형, 메서드명, 매개변수는 그대로 사용
내용만 수정

@Override -> 어노테이션
컴파일러한테 오버라이드된 메서드라고 알려주는 것


다형성 : 상속 + 오버라이딩 + 타입변환
부모타입의 변수에 자식객체들이 들어올 수 있다.
부모타입으로 자식객체를 다룰 수 있는 것.
코드를 수정하지 않고 기능을 확장할 수 있다.

부모타입의 변수에 넣어도 자식클래스에 오버라이딩된 메서드가 실행된다.




추상화
중요한 공통점만 뽑아내고, 불필요한 세부부분은 감추는 것
추상클래스
공통된 특징은 가지고 있지만, 그 자체로 완전한 객체가 되기 어려운 클래스
- 공통적인 필드
- 공통적인 메서드

자식클래스들이 추상클래스를 상속해서 구체적인 내용을 작성



sealed : 봉인된 클래스
permit : 내가 허용한 클래스만 상속을 할 수 있게 한다.



인터페이스
일반 필드
상수, 추상메서드
동작에 대한 가이드라인 제공
같은 범주에 있는 것들이 아니어도 됨

추상클래스와 마찬가지로 직접 객체를 만드는 것이 불가능하다.
인터페이스를 구현할 클래스가 있어야 함

추상메서드를 반드시 오버라이딩해야 한다.

다중구현이 가능하다.

각 인터페이스에 있던 추상메서드를 전부 오버라이딩 해야 한다.

다중 상속이 가능하다.
- 상속은 인터페이스끼리만 가능함




중첩클래스
- 많이 사용되지는 않지만 특정한 상황에서 쓰인다.
1. 클래스 내부에서만 쓰는 전용 객체


정적내부클래스
내부클래스인테 static이 붙은 형태
바깥 객체에 소속된 내부 클래스처럼 보이지만,
실제로는 바깥 객체 없이도 독립적으로 만들 수 있는 내부 클래스



중첩클래스
- 인스턴스 내부 클래스 : 외부클래스의 내부에서 만들어지는 클래스
- 정적 내부 클래스(static) : builder 패턴
- 지역 내부 클래스 : 외부클래스의 메서드 내부에서 만들어지는 클래스
특징
외부에 노출되지 않아도 된다.

익명클래스
이름이 없는 클래스
인터페이스와 같이 사용되는 경우가 많다.
인터페이스는 직접 객체를 생성할 수 없기 때문이다.

//인터페이스
interface InterA {}

//인터페이스를 구현한 클래스
class Impl implements InterA {}

class Main {
	main() {
		Impl impl = new Impl(); //인터페이스를 구현한 클래스의 객체
		InterA inter = new InterA() {
			//이 영역이 익명 클래스
		};
	}
}

인터페이스
구현클래스타입 -> 인터페이스타입

- 어떤 메서드를 정의하려고 하는 건지 먼저 생각할 것
- 정의할 때는 매개변수에 값이 들어 있는게 아님
- 특정 타입의 값만 전달하세요
- 매개변수에 어떤 값이 들어올 지 예상하고 안에 기능을 만들어야 함


메서드 구조
접근제한자 반환타입 메서드명(매개변수) {
	return 반환값;
}


라이브러리, 모듈
- 클래스, 인터페이스의 모음
- JAR 파일로 존재한다.
- 자주 재사용하는 클래스, 인터페이스, 메서드 같은 것들을 추출해서 관리한다.

모듈
- 패키지 관리 기능까지 포함된다.
- JAR파일을 등록하거나, 프로젝트 자체를 참조하는 방법


예외처리
try {
  예외가 발생할 수 있는 코드
} catch(매개변수) {
  예외가 발생하면 실행할 코드
} finally {
  예외가 발생하던 발생하지 않던 무조건 실행되는 코드
}

예외 던지기(throws)
메서드가 내부에서 예외가 밠행했을 때, 내부에서 처리하는게 아니라
메서드를 호출한 쪽에서 처리를 한다.

사용자 정의 예외 만들기
문법상의 오류는 없으나 내 프로그램상 허용이 안돼서 예외를 강제로 발생시키는 것


Object 클래스 (모든 클래스의 최상위 클래스)
equals() : 주소가 같은지 비교
hashcode() : 주소를 반환
toString() : 화면에 출력을 어떻게 할 것인지


nanoTime()
정확한 시간을 측정하기 위한 메서드
나노 10억ns = 1sec


자바에서 제공하는 클래스
- System
- Math
- StringBuilder
- Wrapper
- LovalDateTime, LocalDate, LocalTime
- regex.Pattern












